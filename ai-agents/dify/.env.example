# Copy to .env and fill. .env is gitignored.
DIFY_DB_PASSWORD=change-me-strong-password
# openssl rand -base64 42
DIFY_SECRET_KEY=change-me-base64-secret
# shared secret between api/worker and the sandbox
DIFY_SANDBOX_KEY=change-me-sandbox-key
